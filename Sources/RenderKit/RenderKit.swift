import Foundation

/// Reusable Metal render pipeline, shaders, and image filters.
/// Build out when you reach the first Metal app (#47 chart, #1 long-exposure, #30 upscaler).
/// Keep .metal shader files in this target's resources.
public enum RenderKit {
    public static let info = "Metal shaders, render passes, image filters."
}
